extends _layouts/default

block append config
  - var pagetitle = "Vue"
  - var bodyClass = '_padding-top'
  -
    var breadcrumbs = [
      {
        label: 'Главная',
        link: '#',
      },
      {
        label: 'Страница',
      },
    ]

block append head
  if !isDev
    link(href="css/critical/main-page.css" rel='stylesheet')

block append styles
  link(href="/src/scss/main-page.scss" rel='stylesheet')

block append scripts
  script(src="/src/js/initVue.js" type="module" async)

block content
  .container
    h1.screen-reader-only Схема аэропорта
    //- id must be equal vue root component file name
    #Scheme.js-vue
